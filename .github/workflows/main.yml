on: 
  push:
    branches:
      - loja-da-criacao

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3
    
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.lojadacriacao.com.br
          username: ${{ secrets.ftp_user_loja }}
          password: ${{ secrets.ftp_password_loja }}
          server-dir: /public_html/novo-site/wp-content/themes/lojadacriacao/
