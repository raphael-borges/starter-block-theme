
on: 
  push:
    branches:
      - loja-da-criacao
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server:  ${{ secrets.ftp_host_loja }}
        username:  ${{ secrets.ftp_user_loja }}
        password: ${{ secrets.ftp_password_loja }}
        server-dir: /public_html/novo-site/wp-content/themes/lojacriacao/
